export type ENV = {
  VERSION: string;
  NODE_ENV: string;
  HOST: string;
  PORT: number;
  CORS_ORIGIN: string;
  DATABASE_DOMAIN: string;
  DATABASE_USER: string;
  DATABASE_PASSWORD: string;
  DATABASE:string;
};
